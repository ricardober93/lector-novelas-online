meta {
  name: Ver perfil actual
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/user
  auth: inherit
}

headers {
  cookie: {{authCookie}}
}

tests {
  test("responde 200", function () {
    expect(res.status).to.equal(200);
  });
}
